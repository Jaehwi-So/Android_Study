<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sjh.a26_permission">
    <!--권한 등록-->
    <uses-permission android:name="android.permission.INTERNET"/>   <!-- 인터넷 이용 -->
    <uses-permission android:name="android.permission.CALL_PHONE"/> <!-- 전화 걸기 -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>  <!--주소록 접근-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>